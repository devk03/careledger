export { MANAGED_VAULT_FORMAT_V2, MANAGED_VAULT_WIRE_VERSION,
  MANAGED_VAULT_CHUNK_BYTES, MAX_MANAGED_VAULT_BYTES,
  MAX_MANAGED_VAULT_WIRE_BYTES,
  ManagedVaultWireV2Error, encodeManagedVaultBlobV2,
  decodeManagedVaultBlobV2 } from "@adeno/contracts";
export type { ManagedVaultBlobV2, ManagedVaultChunkV2 } from "@adeno/contracts";
